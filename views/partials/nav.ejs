<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Restaurant</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <% if (locals.user && locals.user.work === "manager") { %>
        <!-- If user is a manager -->
        <li class="nav-item d-flex gap-2 align-items-center">
          <a class="nav-link" href="/menu/create">Create Menu</a>
          <a class="nav-link" href="/menu">Menu</a>
        </li>
        <li class="nav-item">
          <form action="/person/logout" method="POST" class="d-inline">
            <button class="btn btn-outline-light" type="submit">Logout</button>
          </form>
        </li>

        <% } else { %> 
        <% if (locals.user) { %>
        <!-- If user is logged in but not a manager -->
        <li class="nav-item">
          <a class="nav-link" href="/menu">Menu</a>
        </li>
        <li class="nav-item">
          <form action="/person/logout" method="POST" class="d-inline">
            <button class="btn btn-outline-light" type="submit">Logout</button>
          </form>
        </li>
        <% } else { %>
        <!-- If user is not logged in -->
        <li class="nav-item">
          <a class="nav-link" href="/person/signin">Login</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/person/signup">Signup</a>
        </li>
        <% } %> 
        <% } %>
      </ul>
    </div>
  </div>
</nav>
